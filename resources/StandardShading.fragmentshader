#version 330 core

// Interpolated values from the vertex shaders

in vec3 Fposition;
in vec2 Fuv;
in vec3 Fnormal;

// Ouput data
out vec4 fcolor;

uniform mat4 mvpMatrix;

void main(){
	fcolor = vec4(0.f,1.f,0.f, 1.f);

}